<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Düğme arka plan rengi -->
    <solid android:color="#F0F2F1" />

    <!-- Düğme kenar yuvarlaklığı -->
    <corners android:radius="800dp" />

    <!-- Düğme kenar rengi ve kalınlığı -->
    <stroke
        android:color="@color/GreyColor"
        android:width="1dp" />
</shape>